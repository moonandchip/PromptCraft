# Copy to terraform.tfvars and fill in. terraform.tfvars is gitignored.

# Required: GitHub repo in "owner/repo" format. Scopes which repo can
# assume the CI role via OIDC.
github_repo = "OWNER/PromptCraft"

# Optional overrides:
# aws_region   = "us-east-1"
# project_name = "promptcraft"
